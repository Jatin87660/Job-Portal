<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
      <h1>Matching Jobs</h1>

<% if (jobs.length === 0) { %>
  <p>No strong matches found.</p>
<% } else { %>
  <ul>
    <% jobs.forEach(job => { %>
      <li>
        <h3><%= job.title %> - <%= (job.similarity * 100).toFixed(1) %>% match</h3>
        <p><strong>Role:</strong> <%= job.role %></p>
        <p><strong>Location:</strong> <%= job.location %></p>
        <p><%= job.description.slice(0, 300) %></p>
      </li>
    <% }) %>
  </ul>
<% } %>
    
</body>
</html>